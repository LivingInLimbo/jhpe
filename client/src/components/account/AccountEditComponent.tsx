type User = {
  id: number;
  phone?: string;
  email: string;
  firstName?: string;
  lastName?: string;
};

export const AccountEditComponent = ({ user }: { user: User }) => {
  return <div></div>;
};
